# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Edge Functions (deployed separately, need service role key there)
# SUPABASE_SERVICE_ROLE_KEY is ONLY used in Edge Functions, never in frontend

